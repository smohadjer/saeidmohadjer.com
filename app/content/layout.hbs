<!-- {{this.pageId}} -->
<!doctype html>
<html class="no-js{{#if (eq this.pageId 'index')}} home{{/if}}" lang="en">
	<head>
    {{> partials/meta }}
    {{> partials/styles }}
	</head>
	<body>
	{{> partials/header pageId=this.pageId }}
{{> content }}
    {{> partials/footer }}
    {{> partials/scripts }}
	</body>
</html>
